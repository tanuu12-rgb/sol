{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
"build": "pnpm --filter @workspace/crisisiq run build",
    "typecheck:libs": "tsc --build",
"typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"!./artifacts/mockup-sandbox\" --if-present run typecheck"  },
  "private": true,
  "devDependencies": {
    "cross-env": "^10.1.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
